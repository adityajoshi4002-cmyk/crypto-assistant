# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the model (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Optional: default quote currency for prices (defaults to usd)
# CRYPTO_CURRENCY=usd